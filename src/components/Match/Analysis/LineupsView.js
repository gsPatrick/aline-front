import styles from './SubViews.module.css';

export default function LineupsView({ match }) {
    // A implementação do campo tático é complexa.
    // Usaremos um placeholder por enquanto para manter a estrutura funcional.
    return (
        <div className={styles.subContainer}>
            <div className={styles.empty}>
                Componente de "Onzes Iniciais" com o campo tático será implementado aqui.
            </div>
        </div>
    );
}